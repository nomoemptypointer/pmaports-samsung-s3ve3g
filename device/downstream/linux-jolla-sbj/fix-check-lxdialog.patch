Based on patch from https://wiki.postmarketos.org/wiki/Porting_to_a_new_device#Make_kconfig_check_happy

Index: kernel-adaptation-sbj-3.4.108.20200929.1/scripts/kconfig/Makefile
===================================================================
--- kernel-adaptation-sbj-3.4.108.20200929.1.orig/scripts/kconfig/Makefile
+++ kernel-adaptation-sbj-3.4.108.20200929.1/scripts/kconfig/Makefile
@@ -183,6 +183,8 @@ clean-files	+= zconf.tab.c zconf.lex.c z
 clean-files     += mconf qconf gconf nconf
 clean-files     += config.pot linux.pot
 
+HOSTLOADLIBES_mconf   = $(shell $(CONFIG_SHELL) $(check-lxdialog) -ldflags $(HOSTCC))
+
 # Check that we have the required ncurses stuff installed for lxdialog (menuconfig)
 PHONY += $(obj)/dochecklxdialog
 $(addprefix $(obj)/,$(lxdialog)): $(obj)/dochecklxdialog
@@ -208,8 +210,6 @@ HOSTLOADLIBES_gconf	= `pkg-config --libs
 HOSTCFLAGS_gconf.o	= `pkg-config --cflags gtk+-2.0 gmodule-2.0 libglade-2.0` \
                           -Wno-missing-prototypes
 
-HOSTLOADLIBES_mconf   = $(shell $(CONFIG_SHELL) $(check-lxdialog) -ldflags $(HOSTCC))
-
 HOSTLOADLIBES_nconf	= -lmenu -lpanel -lncurses
 $(obj)/qconf.o: $(obj)/.tmp_qtcheck
 
Index: kernel-adaptation-sbj-3.4.108.20200929.1/scripts/kconfig/lxdialog/check-lxdialog.sh
===================================================================
--- kernel-adaptation-sbj-3.4.108.20200929.1.orig/scripts/kconfig/lxdialog/check-lxdialog.sh
+++ kernel-adaptation-sbj-3.4.108.20200929.1/scripts/kconfig/lxdialog/check-lxdialog.sh
@@ -40,7 +40,7 @@ trap "rm -f $tmp" 0 1 2 3 15
 check() {
         $cc -x c - -o $tmp 2>/dev/null <<'EOF'
 #include CURSES_LOC
-main() {}
+int main() { return 0; }
 EOF
 	if [ $? != 0 ]; then
 	    echo " *** Unable to find the ncurses libraries or the"       1>&2
